import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Analytics } from "@vercel/analytics/react";
import { SpeedInsights } from "@vercel/speed-insights/next";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "HistoryGlobe - Explore Historical Sites Worldwide",
  description: "Interactive 3D globe for exploring historical sites worldwide. Discover castles, ancient ruins, Roman forts, UNESCO heritage sites and more.",
  openGraph: {
    title: "HistoryGlobe - Explore Historical Sites Worldwide",
    description: "Interactive 3D globe for exploring historical sites worldwide. Discover castles, ancient ruins, Roman forts, UNESCO heritage sites and more.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Analytics />
        <SpeedInsights />
      </body>
    </html>
  );
}
